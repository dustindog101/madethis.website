BLOB_READ_WRITE_TOKEN=
# optional: protect the cleanup cron (set in Vercel dashboard if you want it)
CRON_SECRET=
# CLI upload API — generate with: openssl rand -hex 32
CLI_API_KEY=
# optional: salt for hashed IP rate-limit keys (defaults to CLI_API_KEY)
RATE_LIMIT_SALT=
